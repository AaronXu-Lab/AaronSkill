# Project context

The repository already has a typed React component library, Storybook, visual-regression tests, accessibility tests, and theme tokens in source code. DESIGN.md must travel with the repository, not independently. The user asks whether component-exact values should be duplicated manually into DESIGN.md.
