---
name: aw-design-system-gallery
description: 为设计系统设计、重建、规范化或审查框架无关的 Gallery，使组件、样式与 Token、规则、模式和组合场景都能依据各自事实源被准确展示。适用于从零搭建设计系统 Gallery、整理已有 showcase、补齐组件 API 或设计基础对比，以及审查 Gallery 与实现是否漂移；不用于把产品页面、业务夹具、调参面板或营销页面改造成设计系统展厅。
metadata:
  version: "3.1.0"
  author: "aaron_xu"
  creation_context: "为让成熟度不一的设计系统都能获得忠于组件 API、Token、规则与组合模式，便于比较且可持续维护的 Gallery，并避免实现、规范和展示长期漂移而创建。"
---

# AW Design System Gallery

为设计系统建立一份可运行的参考面：Gallery 展示真实的组件 API、样式与 Token、规则、模式和组合场景。它不是产品 mock，也不能靠虚构 props、复制静态值或手写故事掩盖事实源中的问题。

执行前读取 [Design System Gallery 文本工作流](docs/workflow.md)。它是执行事实源；流程图是同步的视觉投影：

![Design System Gallery 工作流程](docs/workflow.svg)

## 范围与授权

- 适用于任何框架或仓库中的设计系统，以及承载其组件、样式、Token、规则、模式或组合场景的 Gallery、Story、Demo 或 Showcase 页面。
- 可处理两种模式：规范化已有 Gallery，或从零建立最小可用 Gallery。根据仓库现状选择，不要默认重建。
- 不把本技能的展示结构套到产品路由、业务页面、营销页、fixture、设计调参器或仅供内部诊断的控制面板。
- 先把目标 section 分类为组件、样式/Token、规则、模式/组合场景或内部调试面，再选择对应事实源与展示结构；不要让所有内容都退化为 props 矩阵。
- 组件 section 必须审计 API；只有用户请求包含组件实现或 API 修改时，才实际重构组件。若未获授权，记录问题并继续展示仍可被诚实表达的部分。
- 若 Gallery 必须虚构 props、隐藏冲突默认值或伪造确定性才能成立，停止实现并交付 API 缺口报告；不要制作误导性的示例。
- 遵守目标仓库规则、现有框架、设计 Token 和依赖政策。Gallery 工作不自动授权新增依赖或修改真实产品用法。

## 核心判断

1. **Gallery 是设计系统事实源的投影。** 组件读实现与公开类型，Token 读定义与模式，规则读规范与约束，组合场景读真实组合契约；不要从旧 Gallery 反推真相。
2. **展示结构服从内容类型。** 组件适合 API 矩阵，Token 适合语义分组与模式对比，规则适合正反例与边界，组合场景适合任务和状态覆盖。
3. **组件 API 服务于真实语义。** 不为了排出整齐矩阵而制造无意义的 `variant`、`size` 或布尔 props。
4. **默认示例是一条完整基线。** 它列出当前示例涉及的主要有效默认值；演示用的初始状态不冒充 API 默认值。
5. **一次只比较一个变量。** 做对比时，除目标轴或模式外，内容、宽度、状态与环境保持一致。
6. **产品语境与展示语境分离。** Gallery 用中性内容；真实调用方保留真实文案、图标、数量和业务类型。

## 执行流程

### 1. 定位目标和证据

读取仓库规则，定位设计系统的公开入口、组件、Token/样式、规范、现有 Gallery 基础设施与代表性调用点。先确认目标是局部 section、多个类别整理，还是 Gallery 从零建设。

已有成熟展示时，选一个结构清楚的组件区作为**结构参考**，但不复制其组件专属 prop 名。没有成熟展示时，不凭其他项目的目录或框架假设目标结构。

### 2. 分类 section 并审计事实源

先分类目标内容：

- **组件：** 读取 [组件 API 设计约束](references/component-api-design.md)，核对默认用例、公开轴、有效默认值、状态、事件、slot 与可访问性契约。
- **样式/Token：** 核对 Token 定义、语义角色、别名关系、模式、单位和实际消费位置；不得把运行时计算值或复制出的色值冒充源定义。
- **规则：** 核对规范、lint/测试约束和实现边界，用正例、反例、理由与适用条件表达，不把规则伪装成 prop。
- **模式/组合场景：** 核对参与组件、组合契约、关键任务与状态；使用中性场景，不重建完整产品页面。
- **内部调试面：** 若只服务于调参、fixture 或诊断，不纳入面向使用者的 Gallery 信息架构。

对组件 section，形成以下事实：

- 组件的常见默认用例；
- 主要视觉轴、内容轴、状态轴和行为轴；
- 每个 prop 的类型、有效默认值和条件关系；
- 受控/非受控状态、事件和可访问性契约；
- 是否存在会让 Gallery 失真的 API 缺口。

轻微命名或结构债务不阻断展示。只有当示例无法真实、确定、互斥地表达 API 时，才视为实质缺口。获准修改时先收敛组件 API 并更新调用点；未获准时停止在缺口处，不擅自改变公开契约。

### 3. 选择 Gallery 模式

- **已有 Gallery：** 保留当地 shell、导航、示例 primitives、状态模式和视觉语言；只整理目标范围。
- **从零建立：** 以当前技术栈建立最小骨架，至少包含稳定的 section、Caption、适合各类内容的展示舞台、主题或背景条件（当设计系统支持时）以及可访问的交互。section 数量足以产生查找成本时再加分类、导航或搜索。

不要为了 Gallery 引入完整文档站框架。复用现有构建入口、路由和 Token；新增依赖必须有目标仓库允许且现有技术栈无法合理完成的理由。

### 4. 建立展示模型并实现

完整展示规则见 [Design System Gallery 展示规则](references/gallery-rules.md)，执行 Gallery 工作时必须读取并遵循。按 section 类型选择展示模型：组件先列 `default`，再按一个主要公开轴一行展开；样式/Token 按语义与模式组织；规则展示正反例和边界；模式/组合场景覆盖代表性任务与状态。

凡是对比，保持 Caption、示例数量和视觉顺序一一对应。交互组件提供最小状态容器以真实演示受控或非受控行为，但不得把示例状态写成组件默认值。

### 5. 同步真实调用点

只有组件 API 实际发生变化时，更新所有范围内调用点、导出、类型和测试，并搜索废弃 prop。Gallery 中性化不应传播到产品界面。

### 6. 验证

发现并运行目标仓库已有的类型检查、lint、测试和构建命令；不要假定 npm 或固定脚本名。随后运行 Gallery，并在适用的主题、视口和交互状态下做视觉检查。

完成前确认：

- 默认 Caption 与实现中的有效默认值一致；
- 每个轴使用真实公开 API 名称，顺序一一对应；
- Token、规则和组合场景可追溯到各自事实源，没有复制值或伪造分类；
- 键盘、焦点、禁用、错误及受控/非受控行为在适用时可验证；
- 组件和 Gallery 都使用项目 Token，没有引入品牌或业务默认值；
- 产品调用点未被展示用占位内容污染；
- 新建 Gallery 能从项目标准入口稳定运行。

## 交付结果

交付与任务范围匹配的设计系统源修改、Gallery 实现、验证结果和仍未解决的事实源/API 缺口。不要顺手规范化无关 section，也不要把一次 Gallery 整理扩张为整个设计系统重写。
